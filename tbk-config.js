// ============================================================
// TBK V114 - Configuration Supabase
// ============================================================
// A adapter avec les valeurs reelles du site TBK si elles ne sont pas deja
// declarees ailleurs dans ton application.

window.TBK_CONFIG = window.TBK_CONFIG || {
  version: "TBK V114 - Reset strict conseille",
  supabaseUrl: window.SUPABASE_URL || "",
  supabaseAnonKey: window.SUPABASE_ANON_KEY || ""
};
